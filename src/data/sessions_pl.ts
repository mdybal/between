/**
 * Polish session data.
 *
 * To add Polish translations, copy the structure from sessions.ts and
 * replace the narrative fields (title, summary, highlights) with Polish text.
 * The id, sessionNumber, date, players, and tags fields should remain unchanged.
 *
 * Until translated, this file re-exports the English data as a fallback.
 */
export { sessions as sessionsPl } from './sessions'
