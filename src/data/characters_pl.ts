/**
 * Polish character data.
 *
 * To add Polish translations, copy the structure from characters.ts and
 * replace the narrative fields (description, background, traits, conditions,
 * privateQuarters[].name, masks[].category, masks[].masks[].name) with
 * Polish text.
 *
 * Until translated, this file re-exports the English data as a fallback.
 */
export { characters as charactersPl } from './characters'
