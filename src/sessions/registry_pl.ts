/**
 * Polish session content registry.
 *
 * To add Polish session content, create files named e.g. session-01_pl.tsx
 * alongside the English versions, then register them here following the same
 * pattern as registry.ts.
 *
 * Until translated, this file re-exports the English registry as a fallback.
 */
export { sessionContentRegistry as sessionContentRegistryPl } from './registry'
